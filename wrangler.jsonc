{
  // Cloudflare Workers "static assets" config — serves the built `dist/` folder
  // (Astro's output) as a static site. Lets `npx wrangler deploy` publish the site.
  "name": "jairaghav",
  "compatibility_date": "2025-06-01",
  "assets": {
    "directory": "./dist"
  }
}
